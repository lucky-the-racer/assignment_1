#!/bin/bash
# List files in current directory
ls

# Change directory
cd /path/to/directory

# Create a new directory
mkdir new_directory

# Remove a file
rm example.txt

# Create a new file
touch new_file.txt

# Remove a directory
rmdir new_directory
